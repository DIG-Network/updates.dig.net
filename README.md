# updates.dig.net
DIG auto-update signed feed — serves the signed delegation.json + manifest.json (the beacon trust feed) over S3+CloudFront at /v1/&lt;channel>/. Signature is the gate, not the transport (#504/#513).
